# Explore Python App Instructions

This repository is a deterministic regression fixture for exploration coverage.

Rules:

1. Keep changes minimal and directly related to the task.
2. Preserve the existing module structure unless the task explicitly requires otherwise.
3. After code changes, run `python -m pytest -q` from the repository root.
4. A valid submission must leave the git working tree clean.
5. A valid submission must include the task changes in git commit history before you finish.
6. Once the requested task changes are committed and tests pass, stop immediately.
