# Explore Python App

This fixture intentionally contains a small but obvious maintainability smell so the
exploration pipeline can produce a stable finding and auto-create a task from it.

## Commands

- Run tests: `python -m pytest -q`
