# Split Python App

This fixture is intentionally small but contains two separate modules so planner split and
dependency chaining can be regression-tested end to end.

## Commands

- Run tests: `python -m pytest -q`
