# Tiny Python App

This is a tiny arithmetic project used by the regression framework.

The repository is intentionally tiny so planner, coder, reviewer, and explorer runs stay deterministic enough for real end-to-end regression coverage.

## Commands

- Run tests: `python -m pytest -q`
